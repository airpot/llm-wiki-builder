interface:
  display_name: "LLM Wiki Builder"
  short_description: "Markdown LLM wiki builder"
  default_prompt: "Use $llm-wiki-builder to build, query, validate, optimize, or publish a durable Markdown LLM wiki, confirming its Delivery Contract before normal initialization."
